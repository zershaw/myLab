<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cbsys.saleexplore.idao.IUserInterestsDAO">

    <select id="getTopKHighest" resultType="com.cbsys.saleexplore.entity.UserInterests">
        SELECT *
        FROM T_USER_INTERESTS
        WHERE userId = #{userId}
        ORDER BY score
        LIMIT #{topk}
    </select>


    <select id="getTopKRecent" resultType="com.cbsys.saleexplore.entity.UserInterests">
        SELECT *
        FROM T_USER_INTERESTS
        WHERE userId = #{userId}
        ORDER BY createdTime desc
        LIMIT #{topk}
    </select>


    <insert id="insert">
        INSERT INTO T_USER_INTERESTS (userId,
                                      categoryId,
                                      score,
                                      createdTime)
        VALUES
        <foreach collection="userInterestsList" separator="," item="item">
            (#{item.userId},
            #{item.categoryId},
            1,
            CURRENT_TIME)
        </foreach>
        ON DUPLICATE KEY UPDATE userId=userId,
        categoryId=categoryId,
        score = score + 1,
        createdTime = CURRENT_TIME

    </insert>

</mapper>
