<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cbsys.saleexplore.idao.IStoreRatingDAO">

    <select id="get" resultType="com.cbsys.saleexplore.entity.StoreRating">
        SELECT *
        FROM
        T_STORE_RATING
        <where>
            <if test="userId != null">
                AND userId = #{userId}
            </if>
            <if test="storeId != null">
                AND storeId = #{storeId}
            </if>
        </where>
    </select>


    <insert id="insert" parameterType="com.cbsys.saleexplore.entity.StoreRating">
        INSERT INTO T_STORE_RATING (userId,
                                    score,
                                    storeId,
                                    createdTime)
        VALUES (#{rating.userId},
                #{rating.score},
                #{rating.storeId},
                #{rating.createdTime})
        ON DUPLICATE KEY UPDATE score=#{rating.score},
                                createdTime=#{rating.createdTime}
    </insert>

    <delete id="delete">
        DELETE FROM T_STORE_RATING
        <where>
            <if test="userId != null">
                AND userId = #{userId}
            </if>
            <if test="storeId != null">
                AND storeId = #{storeId}
            </if>
        </where>
    </delete>

</mapper>